name = "tanuki-media-assistant"
compatibility_date = "2025-01-01"

# Serve the Vite build output as static assets.
# This is the current Cloudflare way to host static / SPA sites on Workers.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
